import React, { Component } from 'react';
import { Dashboard as OverView } from 'components';

export default class Dashboard extends Component {
  render() {
    return (
      <OverView/>
    );
  }
}
